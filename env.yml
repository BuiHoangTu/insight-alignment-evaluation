name: alignment-eval
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - compilers
  - pip
  - pip:
    - -r requirements.txt
    - datasets==3.6.0  # override lighteval deps to use mkqa